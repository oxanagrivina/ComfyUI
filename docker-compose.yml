services:
  comfyui:
    image: ghcr.io/comfyanonymous/comfyui:latest
    container_name: comfyui
    command: >
      python main.py
      --listen 0.0.0.0
      --port 8188
      --cpu
    restart: unless-stopped
    ports:
      - "8188:8188"

